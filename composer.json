{
    "name": "wakeon/storage",
    "description": "Wake On Storage web interface",
    "type": "project",
    "require": {
        "php": ">=8.0",
        "symfony/yaml": "^6.0"
    },
    "autoload": {
        "psr-4": {
            "WakeOnStorage\\": "src/"
        }
    }
}
